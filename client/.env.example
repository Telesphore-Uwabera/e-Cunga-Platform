# Optional. Leave unset for local dev so the app uses /api on the Vite port (5173)
# and the dev proxy forwards to the API at localhost:5000.
#
# Set for production (e.g. Netlify) to your deployed API origin — no trailing slash.
# Active backend: https://e-cunga-platform-b6so.onrender.com
#
VITE_API_URL=https://e-cunga-platform-b6so.onrender.com
